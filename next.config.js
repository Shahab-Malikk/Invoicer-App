/** @type {import('next').NextConfig} */
const nextConfig = {
  // Required for server actions (already enabled in Next 14+, explicit here for clarity)
  experimental: {},
}

module.exports = nextConfig
